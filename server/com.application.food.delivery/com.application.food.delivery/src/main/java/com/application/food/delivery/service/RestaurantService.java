package com.application.food.delivery.service;

import com.application.food.delivery.model.RestaurantEntity;

public interface RestaurantService {
        public boolean createRestaurant(RestaurantEntity restaurant);
}
